services:
  lethe:
    build: .
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - CHANNEL_ID=${CHANNEL_ID}
      - MAX_AGE=${MAX_AGE:-720h}
      - INTERVAL=${INTERVAL:-6h}
